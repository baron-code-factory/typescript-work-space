/**
 * Bar を 取得
 *
 * ### Example
 * ```js
 * console.log(getBar())
 * // => Bar
 * ```
 *
 * @returns string
 */
export function getBar() {
  // cc:subの処理で色々値を返すまで#20;Barを取得;Barってよく出るね
  return 'Bar';
}
